# Quick Fix - Permission Denied

The script needs execute permissions. Run:

```bash
chmod +x build_cluster_aast.sh
./build_cluster_aast.sh
```

Or run it with bash directly:
```bash
bash build_cluster_aast.sh
```

